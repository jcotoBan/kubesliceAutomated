apiVersion: controller.kubeslice.io/v1alpha1
kind: Project
metadata:
  name: {{ kubeslice_project }} #Variable to set. 
  namespace: kubeslice-controller
spec:
  serviceAccount:
    readOnly:
      - reader
    readWrite:
      - admin